{
  "name": "runmax-design-system",
  "version": "0.7.0",
  "description": "Sistema de diseño de RunMax: tokens y componentes compartidos entre la web y, a futuro, la app móvil.",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build -w @runmaxshop/tokens && npm run build -w @runmaxshop/react",
    "check:contrast": "npm run check:contrast -w @runmaxshop/tokens",
    "typecheck": "npm run typecheck -w @runmaxshop/react",
    "storybook": "npm run build && npm run storybook -w @runmaxshop/react",
    "build:storybook": "npm run build && npm run build:storybook -w @runmaxshop/react",
    "verify": "npm run build && npm run check:contrast && npm run typecheck"
  }
}
