import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="Fundamentos/Introducción" />

<div style={{ maxWidth: 760, fontFamily: 'Barlow, system-ui, sans-serif', lineHeight: 1.6 }}>

<div style={{
  background: '#000000',
  color: '#FFFFFF',
  padding: '40px 32px',
  marginBottom: 40,
}}>
  <h1 style={{
    fontFamily: 'Barlow Condensed, sans-serif',
    fontSize: 64,
    lineHeight: 1,
    fontWeight: 800,
    fontStyle: 'italic',
    textTransform: 'uppercase',
    margin: 0,
  }}>
    Sistema de diseño de RunMax
  </h1>
  <p style={{ fontSize: 16, lineHeight: 1.5, margin: '16px 0 0', maxWidth: 520, color: '#B8CE00' }}>
    Una sola fuente de verdad para el color, la tipografía y los componentes de RunMax.
    Hoy la web; mañana también la app móvil.
  </p>
</div>

## Por qué existe

Antes de esto, la marca vivía repartida por el código: **113 colores hexadecimales escritos a
mano** y cuatro verdes distintos compitiendo entre ellos, ninguno mal a propósito. Simplemente
no había un sitio donde mirar.

Ese sitio ahora es Figma. Los valores de este repositorio salen de una auditoría de las mesas
**Componentes site** y **Principales** —306 nodos— y no al revés: si un token tiene que
cambiar, cambia primero en Figma.

De esa auditoría salen las dos decisiones que más se notan:

- **El lima de marca es `#B8CE00`**, el de la landing en producción.
- **El lima no es la acción: la acción es negra.** En el código el lima era
  `--color-primary`; en las mesas el botón primario es negro y el lima es un acento de
  destaque. No es un cambio de valor, es un cambio de rol — y por eso el token se llama
  `text.accent` y no `text.primary`.

La regla que lo protege: **el lima nunca es color de texto sobre claro.** Sobre blanco da
1.8:1, muy por debajo del 4.5:1 de WCAG AA. Es superficie (`bg.highlight`) o tinta sobre
oscuro (`text.accent`).

## Cómo está organizado

- **`@runmaxshop/tokens`** — las decisiones de diseño en JSON neutro (formato
  [DTCG](https://tr.designtokens.org/), el estándar del W3C). De ahí se compilan
  automáticamente las variables CSS, un objeto de TypeScript y una versión para React Native.
- **`@runmaxshop/react`** — los componentes de web, que consumen esos tokens y nada más.

Esa separación es la que permite que la app móvil comparta la marca sin compartir el HTML:
cuando llegue, importará los mismos tokens y pintará sus propios componentes nativos. Los
colores, los tamaños y los radios serán exactamente los mismos porque salen del mismo fichero.

## Las tres reglas

1. **Nunca escribas un color, un tamaño o un radio a mano.** Si no existe el token,
   la conversación es sobre añadir el token, no sobre escapar del sistema.
2. **Usa los tokens semánticos, no los primitivos.** `text.tertiary`, no `color.neutral.600`.
   El semántico dice para qué sirve; el primitivo solo dice cómo es.
3. **La accesibilidad no es una fase posterior.** Cada par de texto y fondo está verificado
   contra WCAG 2.1 por un script que corre en CI, y cada historia de esta documentación se
   audita con axe en el propio navegador (pestaña **Accessibility**, abajo).

## Por dónde empezar

- **Fundamentos → Colores** y **Tipografía**: el vocabulario.
- **Componentes**: cada uno con sus variantes y controles en vivo para trastear con las props.
- **Fundamentos → Ejemplo · Formulario del waitlist**: el formulario que hoy está en
  producción, reconstruido usando solo la librería. Es la prueba de que esto cubre pantallas
  reales y no solo ejemplos de demo.

</div>
